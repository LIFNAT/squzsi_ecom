export const metadata = {
  title: 'Auth | SquishyLand',
  description: 'Login and register for SquishyLand',
};

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
